# Dockerd-View
Terminal-hacker themed, real-time Docker resource dashboard — FastAPI backend reads the local Docker daemon; single-page live UI with ring gauges, per-container sparklines, and an event stream.
